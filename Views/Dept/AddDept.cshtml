@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model EmployeePortal.Models.AddDeptViewModel;

<h1>Add Department</h1>

<form asp-action="AddDept" asp-method="post">

    <div class="rounded-1 bg-body-secondary">
        <div class="mt-3">
            <label class="form-label">Name</label>
            <input class="form-control w-50" asp-for="DeptName" />
        </div>

       @*  <div class="mt-2">
            <label class="form-label">Department Head</label>
            @Html.DropDownListFor(
                m => m.SelectedEmployeeId, Model.Employees,
                "-- Select Employee --",
                new { @class = "form-control w-50" })
        </div> *@
        <div class="mt-2">
            <label class="form-label">Department Head</label>
            <select asp-for="DeptHead" class="form-control w-50"
                    asp-items="ViewBag.Employees">
                <option value="">-- Select Head --</option>
            </select>
        </div>

        <div class="mt-3 d-flex">
            <button class="btn btn-primary" style="width:80px" onclick="">Submit</button>
            <button class="btn btn-danger ms-2" onclick="confirmClear()" style="width:80px">Clear</button>
        </div>
    </div>
</form>